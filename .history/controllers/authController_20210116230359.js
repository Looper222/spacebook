// signup_post
const User = require('../models/User');
const jwt = require('jsonwebtoken');

const signup_post = async (req, res) => {
    const userProps = { email, name, surname, password, phoneNumber, birthDate, race, sex, planet };
    const  = req.body;

    try {

    }
    catch(err) {
        console.log(err);
    }
}
